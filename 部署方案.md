# 销售培训系统 — 生产环境部署方案

> 适用场景：将本地已运行、**含真实业务数据**的系统部署到公司服务器。
> 系统已完整 Docker 化，部署脚手架（`docker-compose.yml` / `deploy.sh` / `nginx/` / `.env.example`）齐全。

---

## 0. 一分钟速览

| 项 | 结论 |
|----|------|
| 部署方式 | Docker Compose 一键起 3 个容器 |
| 必须迁移的数据 | ① **MySQL 业务数据**（mysqldump 导出导入）② **`backend/uploads/` 77G 视频**（rsync 传输） |
| 最大的坑 | 77G 视频不能走 git/镜像；MySQL 端口默认暴露公网需关闭 |
| 部署后默认账号 | `admin / admin123`（首次登录立即改密码） |

---

## 1. 系统架构

`docker-compose.yml`（项目名 `sales-training`）启动 3 个容器：

| 容器名 | 作用 | 数据持久化卷 |
|--------|------|-------------|
| `training-mysql` | MySQL 8.0 数据库 | `sales-training_mysql_data` |
| `training-backend` | FastAPI 后端 (Python 3.12) | `sales-training_uploads_data` |
| `training-nginx` | 唯一入口（80端口） | 挂载前端 dist + uploads（只读） |

**访问路由**（Nginx 统一入口）：

| 路径 | 指向 |
|------|------|
| `/` | 培训端 training-web（移动优先） |
| `/admin` | 管理端 admin-web |
| `/api/*` | FastAPI 后端反向代理 |
| `/uploads/*` | 静态视频/图片文件 |

**技术栈**：Vue 3（管理端 Element Plus / 培训端 Vant）+ FastAPI + SQLAlchemy Async + MySQL 8.0 + Nginx。异步任务（视频处理 / AI 出题）由 FastAPI BackgroundTasks 在后端进程内执行，无独立 worker。

---

## 2. 服务器准备

### 2.1 硬件 / 系统要求

| 资源 | 建议 |
|------|------|
| 操作系统 | Linux（Ubuntu 22.04 / Debian 12 等） |
| 磁盘 | **≥ 150 GB**（77G 视频 + MySQL + 转码临时文件 + 镜像） |
| 内存 | **≥ 4 GB**（ffmpeg 视频转码较吃内存） |
| CPU | ≥ 2 核 |
| 网络 | 放行 80（必要时 443）；其余端口对公网关闭 |

### 2.2 安装 Docker + Docker Compose v2

```bash
curl -fsSL https://get.docker.com | sh
docker --version
docker compose version   # 必须是 v2
```

> ffmpeg 已写进 `backend/Dockerfile`，**无需在宿主机单独安装**。

---

## 3. 上传 / 不上传清单

### ✅ 需要上传到服务器
- **整个代码仓库**（`git clone` 或 `git push` 后服务器 `git pull`）
- **`.env` 文件**（手动创建，**不进 git**，见第 4 节）
- **MySQL 业务数据 dump**（见第 6 节）
- **`backend/uploads/` 77G 视频**（见第 7 节）

### ❌ 不上传 / 不进 git（`.gitignore` 已覆盖）
- `node_modules/`、`dist/`、`.venv/`、`__pycache__/` —— 服务器重新构建
- **`.env`** —— 含密码密钥，绝不进 git ✓
- `backend/uploads/*` —— 单独传 ✓
- `mysql_data/` `uploads_data/` —— Docker 卷，不手动拷
- `.uv-cache/` `.codex/` `.codegraph/` `.reasonix/` `reasonix.toml` `nul` —— 本地工具垃圾文件 ✓

> 建议本地删掉根目录误生成的空文件 `nul`：`rm nul`（无害但碍眼）。

---

## 4. 配置 `.env`（安全重点）

```bash
cd sales-training
cp .env.example .env
nano .env
```

**生产环境必改项**（默认值绝不能用）：

```bash
# 数据库强密码（三处必须一致：见下方 docker-compose 说明）
SALES_TRAINING_DATABASE_URL=mysql+asyncmy://training:<强密码>@mysql:3306/sales_training

# JWT 密钥：openssl rand -base64 48 生成
SALES_TRAINING_JWT_SECRET_KEY=<随机长串>

# CORS：填实际域名/IP，不要用 ["*"]
SALES_TRAINING_CORS_ORIGINS=["http://你的域名或IP"]

# 火山引擎方舟 ARK API Key（AI 出题 / 语音转写 / 话术演练必需）
SALES_TRAINING_AI_API_KEY=ark-真实Key

# 关闭调试
SALES_TRAINING_DEBUG=false
```

> **密码一致性**：`docker-compose.yml` 中 MySQL 的 `MYSQL_ROOT_PASSWORD` / `MYSQL_PASSWORD` 读取 `.env` 同名变量，需保证 root 密码、user 密码、`DATABASE_URL` 里的密码三处对应一致。建议在 `.env` 中显式补充：
> ```bash
> MYSQL_ROOT_PASSWORD=<强密码>
> MYSQL_USER=training
> MYSQL_PASSWORD=<与 DATABASE_URL 里相同的强密码>
> ```

---

## 5. 部署流程（含真实数据迁移）

> 本节是**情况 B：保留本地真实数据**的完整流程。核心区别：建表后**不灌种子数据**，改为导入真实 dump。

### 第 1 步：服务器拉代码 + 配 `.env`

```bash
git clone <仓库地址> sales-training && cd sales-training
cp .env.example .env && nano .env     # 按第 4 节修改
```

### 第 2 步：先只起数据库

```bash
docker compose up -d mysql
docker compose ps        # 等 mysql 健康
```

### 第 3 步：导入 MySQL 真实数据（见第 6 节）

### 第 4 步：传输 uploads 视频（见第 7 节，77G 耗时长，可与第 3 步并行）

### 第 5 步：构建前端 + 起后端/worker/nginx

```bash
# 构建前端 dist
cd admin-web && npm install && npm run build -- --base=/admin/ && cd ..
cd training-web && npm install && npm run build && cd ..

# 构建镜像并启动剩余服务
docker compose build --pull
docker compose up -d
```

### 第 6 步：执行数据库迁移（建表/补结构，**不灌种子**）

```bash
docker compose exec -T backend alembic upgrade head
# ⚠️ 不要运行 seed_all.py（会和导入的真实数据主键冲突）
```

> **关于 `deploy.sh`**：脚本最后一步会自动跑 `seed_data/seed_all.py` 灌默认数据。**情况 B 下不要直接用 `deploy.sh`**，请按本节手动执行；或先把 `deploy.sh` 第 104 行 `... seed_all.py` 注释掉再用。`alembic upgrade head` 那步要保留。

### 第 7 步：验证

```bash
curl http://localhost/health           # 后端健康
docker compose logs -f backend         # 看日志
```

浏览器访问 `http://服务器IP`，用**真实数据中的账号**登录验证。

---

## 6. MySQL 数据迁移

> MySQL 是唯一需要迁移的持久化业务数据库。
> 数据在 Docker 卷里，**不能直接拷文件夹**，必须 `mysqldump`。

### 6.1 本地导出（Windows，当前机器）

本地 MySQL 同样是 Docker 起的，容器名 `training-mysql`：

```bash
docker exec training-mysql mysqldump -u root -p<本地root密码> \
  --single-transaction --default-character-set=utf8mb4 \
  sales_training > sales_training_dump.sql
```

### 6.2 传到服务器

```bash
scp sales_training_dump.sql user@服务器IP:/tmp/
```

### 6.3 服务器导入（在第 5 节第 2 步起好 mysql 容器后）

```bash
docker exec -i training-mysql mysql -u root -p<服务器root密码> \
  sales_training < /tmp/sales_training_dump.sql
```

> 导入顺序很重要：**先导入真实数据，再 `alembic upgrade head`**（补齐结构），**全程不跑 `seed_all.py`**。

---

## 7. uploads 视频迁移（77G）

视频在 `uploads_data` 卷里，对应容器内 `/app/uploads`。

### 7.1 本地 → 服务器（用 rsync，支持断点续传）

```bash
# 在 WSL / Git Bash 等能跑 rsync 的环境执行
rsync -avz --progress backend/uploads/ user@服务器IP:/tmp/uploads/
```

> 77G 视频传输耗时较长，`rsync` 中断后可重复执行续传。也可分批传输或先压缩打包再传。

### 7.2 灌入 Docker 卷

```bash
# 确认卷名
docker volume ls | grep uploads     # 通常是 sales-training_uploads_data

# 把宿主机文件复制进卷
docker run --rm \
  -v sales-training_uploads_data:/dest \
  -v /tmp/uploads:/src \
  alpine sh -c "cp -a /src/. /dest/ && chown -R 0:0 /dest"
```

### 7.3 验证

```bash
docker compose exec backend ls -la /app/uploads | head
# 浏览器访问 http://服务器IP/uploads/<某视频文件名> 应能播放
```

---

## 8. 安全加固（公司服务器必做）

### 8.1 关闭对外暴露的数据库端口 ⚠️ 最高优先级

当前 `docker-compose.yml` 将 `3306`(MySQL) 映射到 `0.0.0.0`，**等于把数据库暴露在公网**。容器间走 Docker 内部网络通信即可，生产环境应：

```yaml
# mysql 服务 —— 删除或改为仅本机
    ports:
      - "127.0.0.1:3306:3306"   # 或整段删除
```

> 后端通过服务名 `mysql:3306` 在内部网络访问，删掉 host 端口映射不影响功能。

### 8.2 其他

| 项 | 做法 |
|----|------|
| HTTPS | Nginx 现仅 80 明文。需 443 + SSL 证书（certbot 或公司证书），改 `nginx/default.conf` |
| 域名 | `nginx/default.conf` 的 `server_name _;` 替换为实际域名 |
| 防火墙 | 仅放行 80/443，其余端口关闭 |
| 默认密码 | 部署后立即修改 `admin / admin123` 及所有默认账号密码 |
| AI 视频 URL | 生产建议为视频配可公网访问 URL，避免大视频走 ffmpeg 压缩转 base64（慢） |

---

## 9. 备份策略（上线后）

```bash
# 数据库每日备份（可配 crontab）
docker exec training-mysql mysqldump -u root -p<密码> \
  --single-transaction sales_training | gzip > backup_$(date +%F).sql.gz

# uploads 卷定期打包备份
docker run --rm -v sales-training_uploads_data:/src -v $(pwd):/bak \
  alpine tar czf /bak/uploads_backup.tar.gz -C /src .
```

---

## 10. 日常运维命令

```bash
docker compose logs -f              # 实时日志
docker compose logs -f backend      # 单服务日志
docker compose ps                   # 容器状态
docker compose restart              # 重启全部
docker compose restart backend      # 重启单服务
docker compose down                 # 停止（保留数据卷）
docker compose up -d                # 启动

# 更新部署（情况 B 下注意 deploy.sh 的 seed 问题）
git pull && docker compose build && docker compose up -d
docker compose exec -T backend alembic upgrade head   # 如有新迁移
```

---

## 11. 部署检查清单

- [ ] 服务器装好 Docker + Compose v2，磁盘 ≥150G
- [ ] `.env` 已创建，所有默认密码/密钥/AI Key 已改
- [ ] MySQL 端口映射已关闭（8.1）
- [ ] 先起 mysql → 导入 MySQL dump → `alembic upgrade head`（不跑 seed）
- [ ] 77G uploads 已 rsync 并灌入卷，视频可正常访问
- [ ] 前端 dist 构建完成，`/` 和 `/admin` 可访问
- [ ] `curl http://localhost/health` 正常
- [ ] 用真实账号登录验证数据完整
- [ ] 默认账号密码已修改
- [ ] 配置 HTTPS（如公司要求）
- [ ] 配置数据库 + uploads 定时备份
