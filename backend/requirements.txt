# ── Python dependencies ───────────────────────────────────────────────
# ── System dependencies (not pip-installable) ────────────────────────
#   ffmpeg (含 ffprobe) — 视频上传后自动压缩为手机端 H.264 MP4
#   安装: brew install ffmpeg / apt install ffmpeg / winget install ffmpeg

fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
sqlalchemy[asyncio]==2.0.35
asyncmy==0.2.10
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.12
alembic==1.13.3
aiofiles==24.1.0
python-dotenv==1.0.1
bcrypt==4.0.1
cryptography==43.0.1
dashscope>=1.20.0
mcp>=1.0.0
pypinyin>=0.55.0
