# 部署交付说明 — 给同事（完整版）

> 这是一份完整的交付说明，覆盖所有你需要知道的内容。不需要再看其他文档。

---

## 你会收到什么

| 交付物 | 形式 | 说明 |
|--------|------|------|
| 代码仓库 | GitHub 地址 | `git clone` 即可，含 `nginx/default.conf` |
| `.env` 文件 | 私发（微信/钉钉） | 含真实密钥，**不进 git，不发邮件** |
| `sales_training_dump.sql` | 文件传输 | 完整业务数据库，含所有视频/题目/用户/进度 |
| `uploads.zip` | 文件传输（约 60M） | 封面图 + 销售音频；**不含视频**（视频已在 OSS） |

**视频全部在阿里云 OSS，服务器不用存视频文件。**

---

## 服务器要求

| 项目 | 要求 |
|------|------|
| 操作系统 | Linux |
| CPU / 内存 | ≥ 2 核 / ≥ 4 GB |
| 磁盘 | ≥ 20 GB |
| 软件 | Docker + Docker Compose v2、Git、Node.js 18+ |
| 网络（出站） | 需能访问 `*.oss-cn-hangzhou.aliyuncs.com:443`（视频播放/ASR 用） |

---

## 部署流程（按顺序执行）

### 第 1 步：安装 Docker + Node.js

```bash
# Docker
curl -fsSL https://get.docker.com | sh
docker --version && docker compose version   # 确认 Compose v2

# Node.js 18（用于构建前端）
curl -fsSL https://deb.nodesource.com/setup_18.x | bash -
apt-get install -y nodejs
node --version
```

### 第 2 步：拉取代码

```bash
git clone https://github.com/zhenqi0401/sales-training.git
cd sales-training
git checkout release/v1.0
```

### 第 3 步：创建 `.env`

把我私发给你的 `.env` 文件放到项目根目录（和 `docker-compose.yml` 同级）：

```bash
ls .env   # 确认存在
```

**生产必改项**（`.env` 里找到对应行改掉）：

```bash
SALES_TRAINING_DEBUG=false
SALES_TRAINING_JWT_SECRET_KEY=<openssl rand -base64 48 生成的随机串>
SALES_TRAINING_CORS_ORIGINS=["https://你的域名"]

# 数据库密码（三处保持一致）
MYSQL_ROOT_PASSWORD=<强密码>
MYSQL_USER=training
MYSQL_PASSWORD=<强密码>
SALES_TRAINING_DATABASE_URL=mysql+asyncmy://training:<强密码>@mysql:3306/sales_training
```

### 第 4 步：构建前端 dist

```bash
# 管理端
cd admin-web && npm install && npm run build && cd ..

# 培训端
cd training-web && npm install && npm run build && cd ..

# 确认 dist 存在
ls admin-web/dist training-web/dist
```

### 第 5 步：起数据库

```bash
docker compose up -d mysql
# 等约 30 秒，确认健康
docker compose ps   # mysql 应显示 (healthy)
```

### 第 6 步：导入数据库

把我发给你的 `sales_training_dump.sql` 传到服务器，然后：

```bash
# 把 dump 复制进容器
docker exec -i training-mysql mysql \
  -u root -p<你设置的 MYSQL_ROOT_PASSWORD> \
  sales_training < sales_training_dump.sql

# 补齐表结构（如有新字段）
docker compose run --rm backend alembic upgrade head
```

> ⚠️ **不要跑** `seed_all.py`，那是灌默认数据的，会和真实数据冲突。

### 第 7 步：导入 uploads（封面/音频，约 60M）

```bash
# 把 uploads.zip 传到服务器
scp uploads.zip user@服务器IP:/tmp/

# 解压并灌入 Docker 卷
unzip /tmp/uploads.zip -d /tmp/uploads
docker run --rm \
  -v sales-training_uploads_data:/dest \
  -v /tmp/uploads:/src \
  alpine sh -c "cp -a /src/. /dest/"
```

> 不用传视频，视频都在 OSS，播放时走签名 URL 直连。

### 第 8 步：构建镜像并启动全部服务

```bash
docker compose build --pull
docker compose up -d

# 查看状态
docker compose ps
docker compose logs -f backend   # 看有没有报错
```

### 第 9 步：验证

```bash
curl http://localhost/health   # 应返回 {"status":"ok"}
```

浏览器访问 `http://服务器IP`，用真实账号登录，随便点开一个视频，**能播放即成功**（URL 里带 `?Signature=` 说明 OSS 签名正常）。

---

## 你负责的 Nginx / 域名 / HTTPS

`nginx/default.conf` 已在仓库里，按需改：

1. **域名**：把 `server_name _;` 改成你的域名，如 `server_name training.example.com;`
2. **HTTPS**：文件末尾有 443/SSL 注释模板，放开并挂载证书即可
3. **CORS**：同时把 `.env` 的 `SALES_TRAINING_CORS_ORIGINS` 改成对应域名

HTTPS 上线后视频签名 URL 已是 `https://`，**不会触发混合内容拦截**。

---

## 关闭 MySQL 对外暴露（安全必做）

`docker-compose.yml` 里 MySQL 默认把 3306 暴露到公网，生产必须关掉：

```yaml
# mysql 服务，ports 段改为：
ports:
  - "127.0.0.1:3306:3306"   # 或直接删除整个 ports 段
```

改完 `docker compose down && docker compose up -d`。

---

## 常见问题

| 现象 | 原因 / 处理 |
|------|------------|
| 视频 403 AccessDenied | OSS 签名过期（默认 12h）或 `.env` OSS 密钥不对；刷新页面重新签发 |
| 上传视频后发布卡「生成中」 | 服务器出网不能访问 OSS；检查防火墙 443 出站 |
| 封面图不显示 | uploads 没灌进去；检查 `uploads_data` 卷里有没有 `covers/` |
| `/admin` 404 | `admin-web/dist` 没构建；重新 `npm run build` |
| 容器起不来 | 看 `docker compose logs backend`，多半是 `.env` 配置缺失 |

---

## 日常运维

```bash
docker compose logs -f backend       # 实时日志
docker compose restart backend       # 重启后端
docker compose down && docker compose up -d   # 重启全部

# 更新代码
git pull origin release/v1.0
docker compose build backend
docker compose up -d backend
```

---

## 联系我

OSS / AI 出题相关问题：把 `docker compose logs backend` 对应报错发我。
其余问题随时联系。
