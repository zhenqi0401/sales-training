# AGENTS.md

本文件为仓库内 AI coding agent 的长期工作规范。所有 agent 在修改代码、文档、配置或项目结构前，都必须先阅读并遵守本文件。

## 目标

- 交付可运行、可维护、可测试的改动，而不是只给出建议。
- 优先理解现有项目结构和约定，再做最小必要修改。
- 保持变更范围清晰，避免无关重构、格式化和依赖变更。
- 让每次重要变更都有记录、有验证、有可追踪原因。

## 项目上下文优先级

Agent 应按以下顺序理解项目：

1. 当前用户请求和最近对话。
2. 本文件以及更靠近工作目录的 `AGENTS.md`。
3. `README.md`、`docs/`、`update.md`、贡献指南、架构说明。
4. 包管理文件、测试配置、CI 配置和现有代码模式。
5. 最近的 git diff 和相关提交历史。

如不同说明冲突，优先级为：系统/开发者指令 > 用户最新明确要求 > 更近目录的 `AGENTS.md` > 项目文档 > 既有代码风格。

## 工作流程

### 开始前

- 先查看目录结构和相关文件，不要基于文件名猜测实现。
- 对不明确但风险较低的问题，做合理假设并在回复中说明。
- 对会影响架构、数据、权限、安全、生产行为的问题，先提出确认问题。
- 在脏工作区中工作时，不要回滚、覆盖或格式化与任务无关的用户改动。

### 实施中

- 修改范围应贴近用户目标，避免顺手重构。
- 优先复用现有工具、框架、组件、服务和测试辅助函数。
- 新增抽象必须有实际价值：减少重复、降低复杂度或匹配既有边界。
- 不要引入新依赖，除非现有能力无法合理完成任务；新增依赖前说明原因。
- 不要提交密钥、令牌、个人路径、构建产物、日志或大体积临时文件。

### 完成前

- 运行与改动相关的最小验证命令，例如 lint、typecheck、unit test、build。
- 如果无法运行验证，说明原因和剩余风险。
- 检查 diff，确认没有无关文件、格式化噪音或调试代码。
- 在最终回复中说明改了什么、验证了什么、还有什么注意事项。

## 代码规范

- 遵循项目现有语言、框架、目录和命名约定。
- 保持函数和模块职责单一，避免隐藏副作用和隐式全局状态。
- 优先写清楚的代码，不为炫技牺牲可读性。
- 错误处理要显式，错误信息应帮助定位问题。
- 公共 API、复杂业务规则和非显然决策需要简短说明。
- 避免无意义注释，例如逐字复述代码行为。
- 不要删除已有测试，除非测试已被明确替换或需求已变更。

## 测试与质量

- 修复 bug 时，优先补充能复现问题的测试。
- 新增功能时，至少覆盖核心路径和关键边界情况。
- 对共享逻辑、数据转换、权限控制、支付、删除、迁移等高风险区域，应扩大验证范围。
- 不要为了让测试通过而弱化断言或跳过失败测试。
- 如果测试失败，先定位根因，再决定是否修改实现或测试。

## 文档维护

- 当新增命令、环境变量、配置、外部服务、目录结构或开发流程时，同步更新相关文档。
- 如果项目根目录存在 `update.md`，每次重要变更都必须追加记录。
- 如果项目根目录不存在 `update.md`，首次重要变更时创建它。
- `update.md` 记录应包含日期、变更摘要、影响范围和验证方式。

推荐格式：

```markdown
## YYYY-MM-DD

- 变更：简要说明做了什么。
- 影响：说明影响的模块、命令、配置或用户行为。
- 验证：列出已运行的检查；未验证时说明原因。
```

## Git 与文件操作

- 不要执行破坏性命令，例如 `git reset --hard`、无确认删除、大范围覆盖。
- 不要回滚用户已有改动，除非用户明确要求。
- 不要自动提交 git commit，除非用户明确要求。
- 编辑文件时保持最小 diff；避免无关换行、排序、格式化。
- 生成临时文件时放入项目约定的临时目录，并在完成后清理不需要的文件。

## 安全与隐私

- 不读取、打印或提交不必要的敏感信息。
- 不将 `.env`、密钥、证书、令牌或生产数据写入回复或代码。
- 涉及认证、授权、加密、支付、删除、迁移和外部网络调用时，采用保守实现并补充验证。
- 对用户数据处理保持最小化原则，只处理完成任务所需的数据。

## 前端与用户体验

- 遵循现有设计系统、组件库、间距、颜色和交互模式。
- 不新增与产品风格冲突的视觉体系。
- 保证移动端和桌面端不会出现文字溢出、控件重叠或布局跳动。
- 对可交互 UI，考虑 loading、empty、error、disabled、success 等状态。
- 完成显著前端改动后，应尽量通过浏览器实际检查页面。

## 后端与数据

- 保持接口契约稳定；破坏性变更必须说明迁移方式。
- 数据迁移应可重复、可回滚或至少有清晰恢复方案。
- 输入校验应靠近边界层，核心业务逻辑不应假设外部输入可信。
- 日志应有助排障，但不得泄露敏感信息。
- 外部服务调用应处理超时、失败、重试和降级场景。

## 依赖与环境

- 优先使用项目已有包管理器和锁文件。
- 不混用包管理器，例如已有 `pnpm-lock.yaml` 时不要生成 `package-lock.json`。
- 新增环境变量时，同步更新示例配置和文档。
- 不把本机绝对路径写进项目配置，除非项目明确要求。

## Agent 回复规范

- 回复要简洁、具体，优先说明结果和验证。
- 提到文件时给出准确路径。
- 如果做了假设，明确写出假设。
- 如果有失败或未完成项，直接说明原因、影响和建议下一步。
- 不把大量无关命令输出贴给用户，只总结关键结果。

## 常用检查清单

提交最终回复前，agent 应自查：

- 是否满足用户最新请求？
- 是否遵守本文件和更具体目录说明？
- 是否避免改动无关文件？
- 是否更新了必要文档和 `update.md`？
- 是否运行了相关验证？
- 是否清楚说明了未验证或失败的部分？

